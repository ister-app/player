subscription deviceCommands($deviceId: ID!) {
    deviceCommands(deviceId: $deviceId) {
        deviceId
        command
        mediaType
        mediaId
        startId
        playQueueId
        positionInMilliseconds
        timestamp
    }
}
