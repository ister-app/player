subscription playbackCommands($playQueueId: ID!) {
    playbackCommands(playQueueId: $playQueueId) {
        playQueueId
        command
        positionInMilliseconds
        playQueueItemId
        timestamp
    }
}
