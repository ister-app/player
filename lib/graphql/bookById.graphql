query bookById($id: ID) {
    bookById(id: $id) {
        ...fragmentBook
        chapters { ...fragmentChapter }
        resumeChapter { id }
        epubFiles {
            id path mediaOverlays
            directory { node { url } }
        }
        watchStatus { id watched readingLocation readingProgress }
    }
}
