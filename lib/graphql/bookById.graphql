query bookById($id: ID) {
    bookById(id: $id) {
        ...fragmentBook
        chapters { ...fragmentChapter }
        resumeChapter { id }
        epubFiles {
            id path format pageCount mediaOverlays
            directory { servingNode { url } }
        }
        watchStatus { id watched readingLocation readingProgress }
        progress { ...fragmentBookProgress }
    }
}
