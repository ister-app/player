query recentlyPlayedTracksByArtist($id: ID) {
    personById(id: $id) {
        id
        recentlyPlayedTracks(limit: 10) {
            ...fragmentTrack
            lastPlayedAt
            album { ...fragmentAlbum }
        }
    }
}
