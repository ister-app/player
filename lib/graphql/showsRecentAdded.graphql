query shows($page: Int, $size: Int, $sorting: SortingEnum, $sortingOrder: SortingOrder, $libraryId: ID) {
    shows(page: $page, size: $size, sorting: $sorting, sortingOrder: $sortingOrder, libraryId: $libraryId) {
        number
        size
        totalElements
        totalPages
        content {
            id
            releaseYear
            name
            images {
                ...fragmentImages
            }
            episodes {
                number
            }
            metadata {
                ...fragmentMetadata
            }

        }
    }
}
