query podcastEpisodes($podcastId: ID!, $page: Int, $size: Int, $sortingOrder: SortingOrder) {
    podcastEpisodes(podcastId: $podcastId, page: $page, size: $size, sortingOrder: $sortingOrder) {
        content { ...fragmentPodcastEpisode }
        totalPages totalElements number size
    }
}
