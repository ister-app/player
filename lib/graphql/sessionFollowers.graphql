query sessionFollowers($playQueueId: ID!) {
    sessionFollowers(playQueueId: $playQueueId) {
        userId
        userName
        deviceId
        deviceName
        platform
        since
    }
}
