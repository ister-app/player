query rankedBooks($libraryId: ID!, $kind: RankKind!, $page: Int, $size: Int) {
    libraryById(id: $libraryId) {
        id
        rankedBooks(kind: $kind, page: $page, size: $size) {
            number
            size
            totalElements
            totalPages
            content { ...fragmentBook }
        }
    }
}
