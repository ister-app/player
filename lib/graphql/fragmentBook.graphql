fragment fragmentBook on Book {
    id name title releaseYear seriesIndex
    author { id name }
    series { id name }
    images { ...fragmentImages }
    metadata { ...fragmentMetadata }
    rating
}
