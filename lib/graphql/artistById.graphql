query artistById($id: ID) {
    artistById: personById(id: $id) {
        id name birthYear
        images { ...fragmentImages }
        metadata { ...fragmentMetadata }
        credits { ...fragmentPersonCredit }
    }
}
