fragment fragmentSeries on Series {
    id name startYear readingDirection userReadingDirection
    cover { ...fragmentImages }
    images { ...fragmentImages }
    metadata { ...fragmentMetadata }
}
