# CROSS_ORIGIN_ISOLATION=on: make the page cross-origin isolated so
# SharedArrayBuffer (multi-threaded wasm renderer) is available.
add_header Cross-Origin-Opener-Policy "same-origin" always;
add_header Cross-Origin-Embedder-Policy "require-corp" always;
